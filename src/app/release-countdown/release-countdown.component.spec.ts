/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ReleaseCountdownComponent } from './release-countdown.component';

describe('Component: ReleaseCountdown', () => {
  it('should create an instance', () => {
    let component = new ReleaseCountdownComponent();
    expect(component).toBeTruthy();
  });
});
